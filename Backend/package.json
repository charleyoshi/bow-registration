{
  "name": "bowregistrationbackend",
  "version": "1.0.0",
  "description": "db models     students      [            studentID(Number, unique),          username(String, unique),          password(hash?),         firstname(String),          lastname(String),         email(String),         dob(date),         department(String),         program(String)      ]",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "mongoose": "^8.0.0",
    "validator": "^13.11.0"
  }
}
